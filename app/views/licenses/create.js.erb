$('.tab.payments .payment-table tbody').html("<%= j render(@licenses) %>");
$('.new_license')[0].reset();
$('div.payments_block').html("<%= j render(partial: 'form') %>");
$('.notification-top-panel').remove();
