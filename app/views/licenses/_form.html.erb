<div class="card card-tab-content tab payments">
  <div class="card-header">
    <h2 class="card-header-title"><%= t 'plans_and_billings' %></h2>
  </div>
 
  <div class="regular-payments">
  <%# <h3 class="table-title" ><%= t 'please_fill_billing_address_and_other_info' %1> <a href="user/edit" class="link tab-active"><%= t 'personal_info_tab' %1></a> <%= t 'of_my_profile_page' %1></h3> %>

    <%# <h3 class="table-title"><%= t('payment.next_regular') %1></h3> %>
    <%= form_for License.new, validate: true, remote: true do |f| %>
      <table class="regular-payments-table">
        <thead class="builds-table-title">
          <tr>
            <th>License</th>
              <th></th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              <%= f.text_field :token, validate: true %>
            </td>
            <td>
              <button type="submit" class="btn btn-green"><%= t('add') %></button>
            </td>
          </tr>
        </tbody>
      </table>
    <% end %>
    <div id='message_promocode_regular'></div>
  </div>
</div>
